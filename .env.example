# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Optional: Secret to protect the scrape endpoint (set in Vercel environment variables)
CRON_SECRET=""
